
<div id="commentform">
	<%= form_for @comment do |form| %>   <!-- form for generates new form -->
		<%= form.text_field :body %>
		 <!-- <%= form.hidden_field (colon)post_id, value(colon) @post.id%> -->
		<%= form.submit %>
	<% end %>
</div>